name: GitHub Pages Deploy
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x]
    steps:
      - uses: actions/checkout@v3
      - name: test
        env:
           GH_REF: github@nuxse.com/nuxse
        run: |
          echo ${{ secrets.TEST }}
          echo $GH_REF
